# Example environment variables for reviewers.
# Copy this file to ".env" and fill values only if needed.
# Tic-Tac-Toe critical flows do NOT require these values (app is served locally by Playwright webServer).

BASE_URL=
